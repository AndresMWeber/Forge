machine:
  services:
    - docker

dependencies:
    override:
      - pip install docker-compose
    pre:
      - docker build --rm=false -t forge .

test:
  override:
    - docker run forge

deployment:
  production:
    branch: master
