machine:
  services:
    - docker

dependencies:
  override:
    - pip install docker-compose coveralls
  pre:
    - docker build --rm=false -t forge .

test:
  override:
    - docker run -i -v ${PWD}/Forge:/Forge forge
  post:
    - cd /Forge && coveralls


deployment:
  production:
    branch: master
